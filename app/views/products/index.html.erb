<% if !cart.nil? %>
<ul>
  <% cart.each do |item| %>
    <li><%= item %></li>
  <% end %>
</ul>
<% end %>

<%= form_tag("/add", method: "post") do %>
  <%= label_tag(:product, "Name:") %>
  <%= text_field_tag(:product)%>
  <%= submit_tag("add to cart")%>
<% end %>